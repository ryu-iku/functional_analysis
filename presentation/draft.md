1. Bochner's TheoremA continuous function $\phi(\mathbf{z}): \mathbb{R}^d \to \mathbb{R}$ is a positive definite kernel of the form $k(\mathbf{x}, \mathbf{y}) = \phi(\mathbf{x} - \mathbf{y})$ if and only if its Fourier Transform is non-negative.

History of pattern learning
    - linear model
    - non-linear model
    - kernel methods

History of SVM, kernel methods

Why kernel mean embedding?
Comparison between kernel mean embedding and classicla methods
    divergence method
    kernel density estimation
    probabilistic model
    empirical characteristic function approach

Dive into details of how SVM works
    Loss function calculation
    inner product trick -- the calculation could be transfer into inner product
    introduction of Reproducing vector Hilbert space
    prove important two important theorums

The future of SVM and kernel methods


------------
Slides first